# --- Herramientas de desarrollo locales ---
# Carpeta generada por Graphify (indexación/mapa de conocimiento local).
# No forma parte del sitio y no debe subirse al repo.
graphify-out/

# --- Sistema operativo ---
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# --- Editores ---
.vscode/
.idea/
*.swp
*.swo

# --- Node / build de Tailwind ---
node_modules/
npm-debug.log*
dist/
.env

# OJO: styles/tailwind.css SI se versiona a proposito (es el CSS compilado que
# sirve el sitio). Netlify no corre build: publica el repo tal cual. Si alguna vez
# se decide construir en el deploy, agregar aca la linea: styles/tailwind.css

# --- Netlify ---
.netlify
